---
name: openbrain-onboarding
description: Start or resume the OpenBrain conversational onboarding flow.
---

# OpenBrain onboarding

Read `prompts/onboarding.md` from the current vault and follow it one layer at a time. Do not infer consent from a skipped question. In particular, leave `interaction.free_mode` set to `off` unless the user explicitly selects Calibrated Free Mode.
